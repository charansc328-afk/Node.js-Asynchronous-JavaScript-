// V8 executes JavaScript code.
// libuv handles asynchronous operations such as timers and I/O.
// Because of this, Node.js can continue executing other code
// while a timer is running in the background.

setTimeout(() => {
    console.log("Timer finished");
}, 3000);

console.log("Countdown App Ready");
console.log("Node.js can continue running before the timer finishes.");
